node_modules/
data/
*.db
*.db-wal
*.db-shm
uploads/
supabase/.temp/
.env
.env.local
.env.*.local
.env.vercel
.vercel
.playwright-cli/
.worktrees/
output/playwright/
output/admin-reset-links-*.json

# Portable Windows node build output. Only the two artifacts the cloud download
# needs are tracked (farm-node.cjs + sql-wasm.wasm); everything else here is
# regenerated by `npm run build:node` or the download handler.
dist/windows-node/*
!dist/windows-node/farm-node.cjs
!dist/windows-node/sql-wasm.wasm

# Generated slice-verification outputs
verification/out/
